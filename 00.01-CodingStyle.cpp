/*******************************************************
 * 结论：
 * 多数现代 C++ 教程、IDE 默认都是推荐 写法1（大括号和函数在同一行）
 * if语句也如此。
 *******************************************************/
//写法1
int main(){
    // 代码
}             //推荐写法。

//写法2
int main()
{
    // 代码
}


/*******************************************************
 * 推荐空行的位置：
 * 1.函数之间要空行
 *******************************************************/
void foo() {
    ...
}

void bar() {
    ...
}

/*******************************************************
 * 2.函数体内：逻辑分组之间空一行
 *      比如变量声明、数据处理、输出之间.
 *******************************************************/
int main() {
    int cards[13] = { ... };  // 数据定义

    for (int i = 0; i < 13; i++)  // 第一次输出
        cout << cards[i] << " ";
    cout << endl;

    InsertionSort(cards, 13);  // 排序操作

    for (int i = 0; i < 13; i++)  // 第二次输出
        cout << cards[i] << " ";
    cout << endl;

    return 0;
}

/*******************************************************
 * 3.不同语义的语句之间空一行
 *   比如：
 *      定义 vs 使用
 *      输入 vs 处理 vs 输出
 *      初始化 vs 业务逻辑
 *******************************************************/

 /*******************************************************
 * 4.return 前通常空一行（除非函数很短）
 *******************************************************/
...
cout << endl;

return 0;
